[Service]
# RE-ADDED 2026-07-23 with a rate-limit guard inside the script itself
# (see halium-fix-touch-and-unlock.sh) -- an earlier attempt without
# the guard caused the device to become totally unresponsive from
# repeated rmmod/insmod cycling on frequent phosh.service restarts.
ExecStartPre=+/usr/libexec/halium-fix-touch-and-unlock.sh
